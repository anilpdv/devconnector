const initialState = {
  profile: null,
  profiles: null,
  loading: false
};

export default profileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return action.payload;
  }
};
